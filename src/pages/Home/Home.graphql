query GetRates(
  $phrase: String!
) {
  search(query: $phrase, type: REPOSITORY, first: 5) {
    repositoryCount
    nodes {
      __typename
      ... on Repository {
        name
        owner {
          login
        }
      }
    }
  }
}
