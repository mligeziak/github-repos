query GetRepository(
  $owner: String!
  $name: String!
  $cursor: String
) {
  repository(owner: $owner, name: $name) {
    id
    name
    description
    owner {
      id
      login
      avatarUrl
    }
    stargazers {
      totalCount
    }
    defaultBranchRef {
      id
      target {
        ...on Commit {
          history(first: 20, after: $cursor) {
            nodes {
              id
              messageHeadline
              url
              author {
                name
                avatarUrl
              }
              authoredDate
            }
            pageInfo {
              endCursor
              hasNextPage
            }
          }
        }
      }
    }
  }
}
